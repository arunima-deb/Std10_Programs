package com.arunima.stdx.ch4.todo ;

import static com.arunima.util.IOUtil.* ;
import java.util.* ;

/**
 * Write a program to calculate the monthly electricity bill of a consumer
 * according to the units consumed. The tariff is given below: 
 * 
 * Units consumed
 * Charge Up to 100 units 1.25 per unit For next 100 units 1.50 per unit 
 * 
 * More
 * than 200 units 21.80 per unit 
 * 
 * Units consumed = Present reading — Previous
 * reading Use a method name cal (int u) and print the information in the main
 * method as per the given format: Consumer No. Name Units consumed Amount XXX XXX
 * XXX XXX 
 */
public class Program_S_6
{
    public static void main( String[] args )
    {
        println( "----------- Program Title --------------" ) ;

        Scanner sc = new Scanner( System.in ) ;
        println( "<Prompt> : " ) ;

        // Logic

        sc.close() ;
    }
}
