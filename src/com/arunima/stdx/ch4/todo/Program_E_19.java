package com.arunima.stdx.ch4.todo ;

import java.util.* ;

/**
 * Write a class with the name Area using method overloading that computes the area
 * of a parallelogram, a rhombus and a trapezium. Formula: Area of a parallelogram
 * (pg) = base*ht Area of a rhombus (rh) = Frarrae (where, d1 and d2 are the
 * diagonals) Area of a trapezium (tr) = +e + b)*h (where, a and b are the parallel
 * sides, h is the perpendicular distance between the parallel sides) 
 */
public class Program_E_19
{
    public static void main( String[] args )
    {
        System.out.println( "----------- Program Title --------------" ) ;

        Scanner sc = new Scanner( System.in ) ;
        System.out.println( "<Prompt> : " ) ;

        // Logic

        sc.close() ;
    }
}
