package com.arunima.stdx.ch4.todo ;

import static com.arunima.util.IOUtil.* ;
import java.util.* ;

/**
 * Design a class overloading a method calculate( ) as follows: 
 * 
 * (i) void
 * calculate(int m, char ch) with one integer argument and one character argument.
 * It checks whether the integer argument is divisible by 7 or not, if ch is ‘s’,
 * otherwise, it checks whether the last digit of the integer argument is 7 or not.
 * 
 * 
 * (ii) void calculate(int a, int b, char ch) with two integer arguments and one
 * character argument. It displays the greater of integer arguments if ch is ‘g’
 * otherwise, it displays the smaller of integer arguments. 
 */
public class Program_E_22
{
    public static void main( String[] args )
    {
        println( "----------- Program Title --------------" ) ;

        Scanner sc = new Scanner( System.in ) ;
        println( "<Prompt> : " ) ;

        // Logic

        sc.close() ;
    }
}
