package com.arunima.stdx.ch4.todo ;

import static com.arunima.util.IOUtil.* ;
import java.util.* ;

/**
 * Write a menu driven program using a method Number( ) to perform the 
 * 
 * following
 * tasks: 
 * 
 * (i) Accept a number from the user and check whether it is a ‘BUZZ’
 * number or not. A Buzz number is the number which either ends with 7 or is
 * divisible by 7. 
 * 
 * (ii) Accept a number from the user and check whether it is a
 * composite number or not. A composite number has more than one factor (excluding
 * 1 and the number itself). 
 */
public class Program_S_9
{
    public static void main( String[] args )
    {
        println( "----------- Program Title --------------" ) ;

        Scanner sc = new Scanner( System.in ) ;
        println( "<Prompt> : " ) ;

        // Logic

        sc.close() ;
    }
}
