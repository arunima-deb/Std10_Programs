package com.arunima.stdx.ch4.todo ;

import static com.arunima.util.IOUtil.* ;
import java.util.* ;

/**
 * Design a class to overload a function area( ) as follows: (i) double area(double
 * a, double b, double c) with three double arguments, returns the area of scalene
 * triangle using the formula: 
 * 
 * Area = /s(s — a) (s — b's —c), where s = 4+5+¢_
 * 
 * 
 * 2 (ii) double area(int a, int b, int height) with three integer arguments,
 * returns the area of a trapezium using the formula: Area = 5 * height*(a+b) (iii)
 * double area(double diagonall, double diagonal2) with two double arguments,
 * returns the area of a rhombus using the formula: Area = o * (diagonall *
 * diagonal2) [ICSE 2014] 
 */
public class Program_S_11
{
    public static void main( String[] args )
    {
        println( "----------- Program Title --------------" ) ;

        Scanner sc = new Scanner( System.in ) ;
        println( "<Prompt> : " ) ;

        // Logic

        sc.close() ;
    }
}
