package com.arunima.stdx.ch4.todo ;

import java.util.* ;

/**
 * Design a class to overload a method volume( ) as follows: (i) double
 * volume(double r): with radius (r) as an argument, returns the volume of 
 * 
 * sphere
 * using the formula. 
 * 
 * eee MSG 7 rrr 
 * 
 * (ii) double volume(double h, double r):
 * with height(h) and radius(r) as the arguments, returns the volume of a cylinder
 * using the formula. V = Bareh 
 * 
 * (iii)double volume(double 1, double b, double h):
 * with length(1), breadth(b) and 
 * 
 * height(h) as the arguments, returns the volume
 * of a cuboid using the formula. V = I*b*h(length*breadth*height) [ICSE 2018] 
 */
public class Program_E_27
{
    public static void main( String[] args )
    {
        System.out.println( "----------- Program Title --------------" ) ;

        Scanner sc = new Scanner( System.in ) ;
        System.out.println( "<Prompt> : " ) ;

        // Logic

        sc.close() ;
    }
}
